{
  "name": "vuna-calc",
  "version": "1.0.0",
  "description": "VUNA-Calc - A stripped calculator with CI/CD pipeline",
  "scripts": {
    "test": "jest --coverage",
    "lint": "eslint .",
    "build": "node scripts/build.mjs"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": ["src/calculator.js"],
    "coverageThreshold": {
      "global": {
        "branches": 60,
        "functions": 80,
        "lines": 80,
        "statements": 80
      }
    }
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0"
  }
}
